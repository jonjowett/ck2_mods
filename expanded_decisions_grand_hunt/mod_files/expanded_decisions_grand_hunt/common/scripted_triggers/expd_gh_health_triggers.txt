expd_gh_this_character_can_hunt = {
	# This trigger is designed to replace NOT = { health_traits = 1 } in the original decisions
	
	custom_tooltip = {
		text = expd_gh_this_character_can_hunt_tt
		
		# Block hunting if the character is ill in any way
		expd_gh_this_character_has_no_debilitating_health_traits = yes
		
		# Block hunting for characters who are recovering from physical injuries
		NOT = { trait = wounded }
		NOT = { trait = severely_injured }
		
		# Block hunting for characters with severe permanent physical/mental injuries that would seem to make hunting impossible
		NOT = { trait = infirm }
		NOT = { trait = mangled }
		NOT = { trait = maimed }
		is_incapable = no
		
	}
	
	# Final catch-all to ensure that this can't be used for any condition that causes a regency
	has_regent = no
	
}

expd_gh_this_character_has_no_debilitating_health_traits = {
	# No custom_tooltip because this will always be hidden inside the trigger above.
	
	OR = {
		
		# Allow hunting if the character has ZERO traits with is_illness = yes.
		#      NB: This includes all symptoms as well as all illnesses and all epidemics.
		# This part should allow instant partial compatibility with all other mods - no alterations required!
		#      "Partial" compatibility because any newly-added non-debilitating illnesses will still be blockers.
		is_ill = no
		
		# Otherwise, allow hunting if the character's ONLY health traits are those in this list.
		# If another mod, eg. a total conversion, added any new non-debilitating health traits,
		#      they would need to be added to every instance of the list,
		#      and more near-duplicate else_if clauses would need to be added.
		# trigger_if clauses are used here to minimise calculations.
		#      This probably only gives a very small performance improvement,
		#      but all improvements are important,
		#      because the ED:GH decision is available to AI and is checked every 2 months.
		trigger_if = {
			limit = { health_traits == 1 }
			calc_true_if = {
				amount == 1
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 2 }
			calc_true_if = {
				amount == 2
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 3 }
			calc_true_if = {
				amount == 3
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 4 }
			calc_true_if = {
				amount == 4
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 5 }
			calc_true_if = {
				amount == 5
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 6 }
			calc_true_if = {
				amount == 6
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 7 }
			calc_true_if = {
				amount == 7
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 8 }
			calc_true_if = {
				amount == 8
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 9 }
			calc_true_if = {
				amount == 9
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 10 }
			calc_true_if = {
				amount == 10
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 11 }
			calc_true_if = {
				amount == 11
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 12 }
			calc_true_if = {
				amount == 12
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 13 }
			calc_true_if = {
				amount == 13
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else_if = {
			limit = { health_traits == 14 }
			calc_true_if = {
				amount == 14
				trait = stressed
				trait = depressed
				trait = syphilitic
				trait = lunatic
				trait = possessed
				trait = drunkard
				trait = one_eyed
				trait = one_handed
				trait = one_legged
				trait = disfigured
				trait = cancer
				trait = pregnancy_finishing
				trait = hard_pregnancy
				trait = troubled_pregnancy
			}
		}
		trigger_else = {
			always = no
		}
	}
}
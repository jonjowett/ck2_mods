title_decisions = {
	
	expd_at_build_city = {
		ai = no # Tribal AI should never do this
		potential = {
			expd_at_build_city_potential = yes
		}
		from_potential = {
			expd_at_build_city_from_potential = yes
			ai = no
		}
		allow = {
			expd_at_build_city_allow = yes
		}
		effect = {
			expd_at_build_city_effect = yes
		}
	}
	
}
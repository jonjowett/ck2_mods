decisions = {
	
	expd_plots_incompetent_plotters_hate_me_enable = {
		ai = no                               #               AI should not take this decision
		ai_check_interval = 1000              # Just in case (AI should not take this decision)
		potential = {
			NOT = { has_character_modifier = expd_plots_incompetent_plotters_hate_me }
		}
		allow = {
			# Only capable adults can plot
			is_adult = yes
			is_incapable = no
		}
		effect = {
			expd_plots_incompetent_plotters_hate_me_add_modifier_to_this_character = yes
			hidden_tooltip = { character_event = { id = expd_plots_iphm.2000 } }
		}
		ai_will_do = { factor = 0 }            # Just in case (AI should not take this decision)
	}
	
	expd_plots_incompetent_plotters_hate_me_disable = {
		ai_check_interval = 12                 # AI can take this decision, but only as an ultimate fallback. 
		potential = {
			has_character_modifier = expd_plots_incompetent_plotters_hate_me
		}
		allow = {
			# You are always permitted to remove the modifier
			always = yes
		}
		effect = {
			expd_plots_incompetent_plotters_hate_me_remove_modifier_from_this_character = yes
		}
		ai_will_do = { factor = 1 }            # AI will always take this decision
	}
	
}
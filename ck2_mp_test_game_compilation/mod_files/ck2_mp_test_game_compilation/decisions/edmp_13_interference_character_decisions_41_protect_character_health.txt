# Audax Validator EnableCommentMetadata

targetted_decisions = {
	
	edmp_interference_grant_health_to_this_character = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = { NOT = { has_character_modifier = edmp_interference_health } }
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_health_grant
			}
			ROOT = {
				show_scope_change = no
				add_character_modifier = {
					name  = edmp_interference_health
					years = 3
					stacking = no
				}
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_health_grant }
		}
	}
	
	edmp_interference_prolong_health_to_this_character = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = { has_character_modifier = edmp_interference_health }
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_health_prolong
			}
			ROOT = {
				show_scope_change = no
				hidden_tooltip = { remove_character_modifier = edmp_interference_health }
				add_character_modifier = {
					name  = edmp_interference_health
					years = 3
					stacking = no
				}
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_health_prolong }
		}
	}
	
	edmp_interference_revoke_health_from_this_character = {
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = { has_character_modifier = edmp_interference_health }
		allow = {
			ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes }
			ROOT      = { edmp_this_character_is_valid_target_for_negative_actions_by_rootfrom = yes } # Potentially-negative because the character may be relying on this extra health for survival (may die within days if it's unexpectedly revoked)
		}
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_health_revoke
				set_global_flag = edmp_interference_singlechar_bad
			}
			ROOT = {
				show_scope_change = no
				remove_character_modifier = edmp_interference_health
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_health_revoke }
		}
	}
	
	edmp_interference_grant_fertility_to_this_character = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = { NOT = { has_character_modifier = edmp_interference_fertility } }
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_fertility_grant
			}
			ROOT = {
				show_scope_change = no
				add_character_modifier = {
					name  = edmp_interference_fertility
					years = 3
					stacking = no
				}
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_fertility_grant }
		}
	}
	
	edmp_interference_prolong_fertility_to_this_character = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = { has_character_modifier = edmp_interference_fertility }
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_fertility_prolong
			}
			ROOT = {
				show_scope_change = no
				hidden_tooltip = { remove_character_modifier = edmp_interference_fertility }
				add_character_modifier = {
					name  = edmp_interference_fertility
					years = 3
					stacking = no
				}
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_fertility_prolong }
		}
	}
	
	edmp_interference_revoke_fertility_from_this_character = {
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = { has_character_modifier = edmp_interference_fertility }
		allow = {
			ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes }
			ROOT      = { edmp_this_character_is_valid_target_for_negative_actions_by_rootfrom = yes } # Potentially-negative because the character may be relying on this extra health for survival (may die within days if it's unexpectedly revoked)
		}
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_fertility_revoke
				set_global_flag = edmp_interference_singlechar_bad
			}
			ROOT = {
				show_scope_change = no
				remove_character_modifier = edmp_interference_fertility
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_fertility_revoke }
		}
	}
	
	edmp_interference_heal_disease = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = {
			# Hopefully, if a mod changes these triggers, then it will change the equivalent effects (below) in the same way.
			OR = {
				always = no # If everything below is broken (eg. due to a total conversion mod), ensure that this decision is not visible.
				has_disease_trigger = yes
				has_symptom_trigger = yes
			}
		}
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_heal_disease
			}
			ROOT = {
				show_scope_change = no
				# Warn the admin that immunity will NOT be granted
				custom_tooltip = { text = edmp_interference_heal_disease_no_immunity_gained_tt }
				# Hopefully, if a mod changes these effects, then it will change the equivalent triggers (above) in the same way.
				remove_disease_trait_effect = yes
				remove_symptoms_effect = yes
				hidden_tooltip = {
					remove_disease_flags_effect = yes
					remove_treatment_modifiers_effect = yes
				}
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_heal_disease }
		}
	}
	
	edmp_interference_heal_mental = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = {
			OR = {
				
				always = no # If everything below is broken (eg. due to a total conversion mod), ensure that this decision is not visible.
				
				trait = stressed
				trait = depressed
				trait = lunatic
				trait = possessed
				trait = drunkard
				
				has_character_modifier = voice_of_satan
				has_character_modifier = voice_of_jesus
				has_character_modifier = holy_comet
				has_character_modifier = lycanthropy
				
				has_character_flag = demon_child
				has_character_flag = demon_child_fake
				has_character_flag = child_exorcism
				
			}
		}
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_heal_mental
			}
			ROOT = {
				show_scope_change = no
				
				# Show the removal of demon child flags first, in case this is a dealbreaker, because we want to ensure it doesn't fall off the bottom of the tiny dialog window.
				if = {
					limit = {
						OR = {
							has_character_flag = demon_child
							has_character_flag = demon_child_fake
							has_character_flag = child_exorcism
						}
					}
					custom_tooltip = {
						text = edmp_interference_heal_mental_clear_demon_child_flags_tt
						clr_character_flag = demon_child
						clr_character_flag = demon_child_fake
						clr_character_flag = child_exorcism
					}
				}
				
				if = { limit = { trait = stressed  } remove_trait = stressed  }
				if = { limit = { trait = depressed } remove_trait = depressed }
				if = { limit = { trait = lunatic   } remove_trait = lunatic   }
				if = { limit = { trait = drunkard  } remove_trait = drunkard  }
				if = { limit = { trait = possessed } remove_trait = possessed }
				
				if = { limit = { has_character_modifier = voice_of_satan } remove_character_modifier = voice_of_satan }
				if = { limit = { has_character_modifier = voice_of_jesus } remove_character_modifier = voice_of_jesus }
				if = { limit = { has_character_modifier = holy_comet     } remove_character_modifier = holy_comet     }
				if = { limit = { has_character_modifier = lycanthropy    } remove_character_modifier = lycanthropy    }
				
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_heal_mental }
		}
	}
	
	edmp_interference_heal_congenital = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = {
			OR = {
				always = no # If everything below is broken (eg. due to a total conversion mod), ensure that this decision is not visible.
				# Hopefully, if a mod changes this trigger, then it will change the equivalent effect (below) in the same way.
				has_negative_congenital_trigger = yes
			}
		}
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_heal_congenital
			}
			ROOT = {
				show_scope_change = no
				# Hopefully, if a mod changes this effect, then it will change the equivalent trigger (above) in the same way.
				remove_all_negative_congenital_traits_effect = yes
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_heal_congenital }
		}
	}
	
	edmp_interference_heal_maiming = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = {
			OR = {
				always = no # If everything below is broken (eg. due to a total conversion mod), ensure that this decision is not visible.
				trait = maimed
				trait = one_eyed
				trait = one_handed 
				trait = one_legged 
				trait = disfigured 
				trait = mangled
				trait = blinded
				trait = eunuch
			}
		}
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_heal_maiming
			}
			ROOT = {
				show_scope_change = no
				if = { limit = { trait = maimed     } remove_trait = maimed     }
				if = { limit = { trait = one_eyed   } remove_trait = one_eyed   }
				if = { limit = { trait = blinded    } remove_trait = blinded    }
				if = { limit = { trait = one_handed } remove_trait = one_handed }
				if = { limit = { trait = one_legged } remove_trait = one_legged }
				if = { limit = { trait = disfigured } remove_trait = disfigured }
				if = { limit = { trait = mangled    } remove_trait = mangled    }
				if = { limit = { trait = eunuch     } remove_trait = eunuch     }
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_heal_maiming }
		}
	}
	
	edmp_interference_heal_wounds = { # Not potentially-negative
		filter = all_including_me
		ai = no
		from_potential = {
			has_character_flag = edmp_interference_show
			edmp_this_character_can_see_admin_helper_actions = yes
		}
		potential = {
			OR = {
				
				always = no # If everything below is broken (eg. due to a total conversion mod), ensure that this decision is not visible.
				
				trait = wounded
				trait = severely_injured
				trait = infection
				
				has_character_modifier = black_eye
				has_character_modifier = swollen_wrist
				has_character_modifier = swollen_ankle
				has_character_modifier = concussion
				has_character_modifier = cracked_ribs
				has_character_modifier = badly_bruised
				has_character_modifier = sore_muscles
				
			}
		}
		allow = { ROOT_FROM = { edmp_this_character_can_take_admin_helper_actions = yes } }
		effect = {
			hidden_tooltip = {
				ROOT_FROM     = { save_event_target_as = edmp_interference_decision_taker }
				ROOT          = { save_event_target_as = edmp_interference_singlechar_affected }
				set_global_flag = edmp_interference_singlechar_heal_wounds
			}
			ROOT = {
				show_scope_change = no
				
				if = { limit = { trait = wounded          } remove_trait = wounded          }
				if = { limit = { trait = severely_injured } remove_trait = severely_injured }
				if = { limit = { trait = infection        } remove_trait = infection        }
				
				if = { limit = { has_character_modifier = black_eye     } remove_character_modifier = black_eye     }
				if = { limit = { has_character_modifier = swollen_wrist } remove_character_modifier = swollen_wrist }
				if = { limit = { has_character_modifier = swollen_ankle } remove_character_modifier = swollen_ankle }
				if = { limit = { has_character_modifier = concussion    } remove_character_modifier = concussion    }
				if = { limit = { has_character_modifier = cracked_ribs  } remove_character_modifier = cracked_ribs  }
				if = { limit = { has_character_modifier = badly_bruised } remove_character_modifier = badly_bruised }
				if = { limit = { has_character_modifier = sore_muscles  } remove_character_modifier = sore_muscles  }
				
			}
			edmp_interference_notify_players_singlechar = yes
			hidden_tooltip = { clr_global_flag = edmp_interference_singlechar_heal_wounds }
		}
	}
	
}